import { PortOneError } from "../PortOneError.mjs";
export class RestError extends PortOneError {
  data;
  /** @ignore */
  constructor(data) {
    const message = "message" in data ? data.message : void 0;
    super(message);
    this.data = data;
  }
}
